/**
    <--- Recap: Variable, Object, Data types, Loops & Conditions --->
*/

/**
1. const ---> value not going to change
2. let ---> value might change
3. var ---> shoud not use (unless exceptional cases)

*** premetive 
const price = 500 // number
const isPoor = false // Boolean
const sayHi = 'Hello World' // String

*** non-premetive 
cosnt friend = ['Elon', 'Mark', 'Bill', 'Jeff'] // ---> array
const student = {
    class: 9,
    name: "Tomi Khan",
    age: 17,
    address: "Kochukhet"
} // ---> Object

// condition
const price = 500;
if (price = 1500) {
    console.log('Too expensive. not for me');
} else if (price = 1000) {
    console.log('Expensive, are you loosing the rate?');
} else {
    console.log('within my budget');
}

// loop
let pushUp = 0;
while (pushUp < 10) {
    console.log('push up');
    pushUp = pushUp + 1
    pushUp += 1
    pushUp++
}

// for...loop
for(let i = 0; i < 10; i++) {
    console.log(i);
    pushUp = pushUp + 1
    pushUp += 1
    pushUp++
}
 */
